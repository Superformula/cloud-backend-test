import { resolvers, typeDefs } from '../index';

it('resolvers should be defined', () => {
  expect(resolvers).toBeDefined();
});

it('typeDefs should be defined', () => {
  expect(typeDefs).toBeDefined();
});
