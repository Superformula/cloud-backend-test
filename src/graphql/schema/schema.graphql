type Query {
	user(id: ID!): User!
}

type Mutation {
	createUser(data: UserInput!): User!
}

type User {
	name: String!
	location: String!
	description: String
	createdAt: String!
	pictureUrl: String
}

input UserInput {
	name: String!
	location: String!
	description: String
}
