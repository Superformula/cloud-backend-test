// import { isUuid } from 'uuidv4'
// import UserModel from './user.model'

describe(`New UserModel instantiated -> empty constructor`, () =>
{
	it(`When the constructor is empty, the new object MUST have random values for it's properties`, () =>
	{
		// const model = new UserModel()

		// if (!isUuid(model.id)) test.fail(`invalid UserModel ID generated`)
		// if (!model.dob) test.fail(`invalid UserModel DOB generated`)
		// if (!model.name) test.fail(`invalid UserModel NAME generated`)
		// if (!model.address) test.fail(`invalid UserModel ADDRESS generated`)
		// if (!model.createdAt) test.fail(`invalid UserModel CREATED AT generated`)
		expect(true).toBe(true)
	})
})