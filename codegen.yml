overwrite: true
schema: 'http://localhost:4000/graphql'
documents: null
generates:
  src/graphql/types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
